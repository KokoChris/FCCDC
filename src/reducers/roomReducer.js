import  * as types from '../actions/actionTypes';
import initialState from './initialState';

// export default function room (state = {room:initialState.roomState,enemy:initialState.enemyPosition}, action) {
//     switch(action.type) {
//         case(types.GET_ROOM_LAYOUT):
//
//             return state;
//
//
//         default:
//             return state;
//     }

